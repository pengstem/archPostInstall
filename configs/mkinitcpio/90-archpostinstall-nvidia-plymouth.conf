# NVIDIA early KMS and Plymouth for a quiet graphical boot.
# This complete array override intentionally replaces the default kms hook:
# the out-of-tree NVIDIA modules are loaded explicitly instead of embedding nouveau.
MODULES=(nvidia nvidia_modeset nvidia_uvm nvidia_drm)
HOOKS=(base udev autodetect microcode modconf plymouth keyboard keymap consolefont block filesystems fsck)
